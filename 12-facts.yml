- name: Print facts
  hosts: localhost
  tasks:
    - name: Print facts
      ansible.builtin.debug:
        msg:
          - date_time details: "{{ansible_date_time}} "

          - architecture details: "{{ansible_architecture}}"