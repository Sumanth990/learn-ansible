- name: Register
  hosts: localhost
  tasks:
    - name: Shell
      ansible.builtin.shell:uptime
      register: out

    - name: Print out
      ansible.builtin.debug:
        msg: system time {{out}}