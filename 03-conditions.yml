- name: conditions in ansible
  hosts: localhost
  tasks:
    - name: Print a Hello, John
      ansible.builtin.debug:
        msg: Hello, John
      when: {{ item.value }} == John

    - name: Print a Hello, Ram
      ansible.builtin.debug:
        msg: Hello, Ram
      when: {{ item.value }} == Ram