- name: Ignore demo
  hosts: localhost
  tasks:
    - name: Run shell command
      ansible.builtin.shell: exit 1
      ignore_errors: true

    - name: Say Hai
      ansible.builtin.debug:
        msg: Hai